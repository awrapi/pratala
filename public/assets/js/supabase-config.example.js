window.PRATALA_SUPABASE_CONFIG = {
  url: "https://your-project-ref.supabase.co",
  anonKey: "your-public-anon-key"
};

// Copy this file to assets/js/supabase-config.js and replace the placeholders.
// Use only the public anon key. Never expose the service role key.
